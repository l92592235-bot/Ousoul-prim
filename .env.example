# Neon/Postgres connection string (required)
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# Secret password protecting the /admin panel used to generate client links (required)
ADMIN_SECRET=change-me-to-a-long-random-value
